---
title: 'Image to Video - First/Last Frame Mode - Query Task'
api: 'GET https://llm.ai-nebula.com/nebula/v4/api/textOrImage2videoQuery/{taskId}'
---

## Introduction

Query first/last frame mode task status and result.

## Authentication

<ParamField header="Authorization" type="string" required>
  Bearer Token, e.g., `Bearer sk-xxxxxxxxxx`
</ParamField>

## Request Parameters

<ParamField path="taskId" type="string" required>
  Task ID, returned by submit task interface
</ParamField>

### Response Example

<ResponseExample>
```json
{
  "code": 200,
  "msg": "Operation successful",
  "data": {
    "id": "task-1234567890",
    "status": "succeeded",
    "url": "https://example.com/video.mp4"
  }
}
```
</ResponseExample>
